#ifndef CLIENT_H
#define CLIENT_H   
#include <iostream>
#include <string>

using namespace std;

// money laundering?
// user managing to change the BPAY amount limit from $2000 to $50,000
// user must create a account and have the option to delete all the account date
// needs user details such as DOB address and pin passwords and emails?
// how should the client ID be written? should it be the constant int of the user name sting???

class Client{
    private: // use inheritence???
        string Client_Name;
        string Client_Bank_ID; // the account number
        int Client_Age;
        string Client_Address;
        string Client_DOB;
        string Client_Pin;

        // savings account
        // debit account

        password_before
        password_after
        if (password_after != password_before) 

    public:
        Client()
        Client(string C_Name, int C_Age, string)


};
#endif