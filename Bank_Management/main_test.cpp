#include <iostream>
#include <string>

using namespace std;

cout <<"\n Who is the recipient?";
    cin >> Repecient_name;
    if (Repecient_name)

    if (Log_in_name == )


    if (Log_in_name == "George" || Log_in_name == "george"){
        Money_Transfer()
        while (Logged_in == false){
            cout <<"\n George enter your password: ";
            cin >> Log_in_password;
            Logged_in = George.Log_in("George",Log_in_password);
        }
        while (Logged_in == true){
            George.Show_My_Details();
            cout <<"\n Deposit money into account: $";
            cin >> Money_Deposit;
            Account_Money_Deposit = George.Create_Account(Money_Deposit);
            if (Account_Money_Deposit == true){
                while (BPay == false){
                    cout <<"\n Enter receipent name\n";
                    cin >> Repecient_name;
                    cout <<"\n Enter amount $";
                    cin >> Money_BPay;
                    if (Repecient_name == "Wilson" || Repecient_name == "Gary" && Money_BPay > 0){
                        BPay = true;
                    }
                }
                if (Repecient_name == ("Wilson")){
                    George.BPay(Money_BPay,&Wilson);
                    Logged_in = false;
                }
                if (Repecient_name == ("Gary")){
                    George.BPay(Money_BPay,Gary_address);
                    Logged_in = false;
                }
                
            }

        }
    }
    